/*package ejemploFunciones;

public class MainEjemploFunciones {*/
  /*  public static void main() {
        System.out.println("En MainEjemploFunciones");

        System.out.println(MainEjemploFunciones.test());

    int resultadoFuncion = MainEjemploFunciones.test(); // La funcion devuelve un entero por
                                                        // lo tanto la puedo asignar a una variable
                                                        // pero tiene que ser entera.

        //-----------------------------------------------------------------------------------------------------------------------
//    Diseña e implementa las siguientes funciones. Pruebas las funciones llamándolas desde la función Main
//    e incluye el código para la llamada.
//
//    - Una función que no devuelva nada y reciba dos strings pomo parámetros. La función mostrará
//    por pantalla los dos strings.

        public static void funcionVoid(String cadena1, String cadena2) {
            System.out.println("Este es el string 1: " + cadena1 + " y este es el string 2 " + cadena2);

        }

//    - Una función que devuelva un booleano y reciba dos enteros como parámetros. La función devolverá
//    true si los dos números son iguales; false en caso contrario.

        public static boolean sonIguales(int entero1, int entero2) {
            entero1 = 10;
            entero2 = 10;

            return entero2 != entero1 ?  false :  true;

        }

//    - Una función que devuelva un entero y reciba un array de enteros como parámetro.
//    La función devolverá el máximo de los números que contenga el array.
        public static int entero(int[] arrayEnteros) {

            arrayEnteros = new int[]{59, 48, 1, 52, 6, 3};

            int max  = 0;

            for (int i : arrayEnteros){
                if (i > max) {
                    max = i;
                }
            }

            return max;
        }

//    - Una función que devuelva un número en coma flotante y reciba como parámetro un array
//    de números en coma flotante.
//    La función devolverá la suma de los números del array.
//
        public static float numFlotante(float[] nums) {
            System.out.println("returnsumofarray");

            float result = 0;

            for (float num : nums) {
                result += num;
            }

            return result;
        }

//    - Una función que devuelva un String y reciba como parámetro un array de caracteres.
//    La función devolverá un String compuesto por los caracteres, en orden, del array.
//
        public static String cadenaTexto() {

            char [] arrayChars = {'f', 'r', 'e', 's', 'a'};

            for(int i = 0; i < arrayChars.length; i++) {
                return System.out.print(arrayChars(i));
            }
        }

//    - Una función que devuelva un array de enteros y reciba tres enteros como parámetro. La función devolverá un
//    array compuesto de los números pasados como parámetros.
        public static int [] arrayInt(int a, int b, int c) {




            return new int[] {1, 2, 3};
        }


    }

    public static int test() {
        System.out.println("test");
        return 2;
    }

    public static int testParams(int x, String s, boolean b) {
        return 1;
    }

     //Diseña los siguientes prototipos de funciones e incluye la línea de retorno de la función, si aplica.

     //* Una función que no devuelva nada y reciba dos strings como parámetros.
     public static void noDevuelveNada(String string1, String string2) {
         System.out.println("Esta función no devuelve nada");
     }

     // Una función que devuelva un booleano y reciba dos enteros como parámetros.
     public static boolean trueOrFalse(int x, int y) {
         return false;
     }

    // Una función que devuelva un entero y reciba como parámetro un array de enteros.
    public static int unEntero(int[] arrayDeEnteros) {
        return 1;
    }

    // Una función que devuelva un número en coma flotante y reciba como parámetro un array de números en coma flotante.
    public static float numeroFloat(float[] arrayDeFloats) {
        return 1.5f;
    }

    //Una función que devuelva un String y reciba como parámetro un array de caracteres.
    public static String cadenaDeTexto(char[] arrayDeChars) {
        return "String";
    }

    //Una función que devuelva un array de enteros y reciba tres enteros como parámetro.
    public static int [] arrayDeEnteros(int a, int b, int c) {
        return new int[] {1, 2, 3};
    }



}



*/
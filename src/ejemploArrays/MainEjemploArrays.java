package ejemploArrays;

public class MainEjemploArrays {
    public static void main() {
        System.out.println("En MainEjemploArrays");
/*
        int[] numbers = new int[10];
        numbers[0] = 1;

        System.out.println("EL elemento que esta en la posicion 0 es " + numbers[0]);
        System.out.println("EL elemento que esta en la posicion 1 es " + numbers[1]);
        System.out.println("EL elemento que esta en la posicion 0 es " + numbers[11]);

        long [] longs = new long[3];
        float [] floats = new float[11];
        boolean [] booleans = new boolean[2];

        int [] evenNumbers = {
                2, 4, 6, 8, 10
        };

        System.out.println("El primer numero par es " + evenNumbers[0]);
        System.out.println("El segundo numero par es " + evenNumbers[1]);
        System.out.println("El tamaño del array de números es " + numbers.length);
        System.out.println("El tamaño del array de números pares es " + evenNumbers.length);
*/
//EJERCICIOS
/*
        //Declara e inicializa los siguientes arrays:

            // 18 números en coma flotante, simple precisión.
               float [] floats = new float [18];

           // 4 booleanos.
            boolean [] booleanos = new boolean[4];

            // 3 longs.
            long [] longs = new long[3];

            // 4 enteros.
            int[] enteros = new int[4];

            //7 Cadenas de texto.
            String [] cadenas = new String[7];

            //Realiza las siguientes operaciones en los arrays:
            // En el de números en coma flotante:
            // Asigna su primer valor a 3,4 y el último a 8,2.
            // Muestra por pantalla su primer valor y el tercero.

            floats[0] = 3.4f;
            floats[17] = 8.2f;
            System.out.println("El primer numero en coma flotante es " + floats[0]);
            System.out.println("El tercer numero en coma flotante es " + floats[2]);

        System.out.println("\n");
            //En el de booleanos:
            //Asigna true al menos a dos de sus valores.
            //Muestra por pantalla todos sus valores.

            booleanos [1] = true;
            booleanos [2] = true;
        System.out.println(booleanos);


            //En el de longs:
            //Asigna 3.456 como primer valor y 100.000 como segundo.
            //Asigna a su tercer valor la suma del primer valor y el segundo, usando para ello los
            //valores almacenados en el array.
            //Muestra por pantalla el tercer valor.

            longs [0] = 3456;
            longs [1] = 100000;
            longs [2] = (longs [0] + longs [1]);

        System.out.println(longs[2]);

           //En el de enteros:

            //Si el equivalente de la misma posición en el array de booleanos es true, asigna 1 a esa posición;
            // en caso de que sea false, asigna -1.

        enteros[0] = -1;
        enteros[1] = 1;
        enteros[2] = 1;
        enteros[3] = -1;


            //En el de cadenas de texto:
            //Asigna a los seis primeros valores las palabras que quieras.
            //Asigna al último valor el resultado de "unir" el resto de las palabras del array.
            //Muestra el último valor por pantalla.

        cadenas[0] = "faro";
        cadenas[1] = "oso";
        cadenas[2] = "cima";
        cadenas[3] = "tren";
        cadenas[4] = "chopo";
        cadenas[5] = "casa";
        cadenas[6] = cadenas[0] + cadenas[1] + cadenas[2] + cadenas[3] + cadenas[4] + cadenas[5];

        System.out.println(cadenas[6]);

*/
    }
}

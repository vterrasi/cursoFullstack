package ejemploBucles;

public class MainEjemploBucles {
    public static void main() {
        System.out.println("En MainEjemploBucles");

/*
        System.out.println("\nFOR");

        for (int i = 0; i < 10; i++) {
            System.out.println("i es " + i);
        }

        System.out.println("\nWHILE");
            int i = 0;
            while (i < 10) {
            System.out.println("i es " + i);
            i++;
        }

        System.out.println("\nDO WHILE");
        i = 0;
        do {
            System.out.println("i es " + i);
            i++;
        } while (i < 10);

        int[] evenNumbers = {
                2, 4, 6, 8, 10
        };

        System.out.println("\nFOR EACH");
        for (int evenNumber : evenNumbers) {
            System.out.println(evenNumber);
        }

        System.out.println("\nTEST BREAK Y CONTINUE");
        i = 0;
        while (i < 10) {
            if (i % 2 == 0) {
                System.out.println("Es par, me salto tres");
                i += 3;
                continue;
            } else if (i == 7) {
                System.out.println("Es el siete, FIN");
                break;
            } else {
                System.out.println("No es par, es " + i);
                break;
            }
        }
        i++;
//------------------------------------------------------------------------------------------------------
        //EJERCICIOS:

        //Usa un bucle for para recorrer de principio a fin un array de enteros de 20 elementos
        // y mostrarlos por pantalla.


        for (int enteros = 0; enteros < 20; enteros++) {
            System.out.println("Este entero es " + enteros);
        }

        //Usa un bucle while para mostrar cada número de un array de enteros mientras
        // que el número a mostrar no sea un 5.

        int[] enterosWhile = new int[10];
            int i = 0;
            while (i < 10)
                if (enterosWhile[i] != 5) {
                    System.out.println("El numero es " + enterosWhile);
                  }


        //Usa un bucle while para mostrar el contenido de un array de caracteres mientras
        // que el carácter no sea una 'c'.
/*
        char[] arrayDeCaracteres = {
                    'l', 'm', 'd', 'h', 'p', 'c'
            };

            while (char caracter : arrayDeCaracteres)
                if (caracter != 'c') {
                    System.out.println("El caracter es " + caracter);

                }

            //Usa un bucle for each para recorrer un array de números short y mostrarlos.

            short[] shortNumbers = {
                            10, 20, 30, 40, 50
            };

            for (short shortNumber : shortNumbers) {
                System.out.println(shortNumber);
            }

        //Usa un bucle apropiado para recorrer un array de booleanos y mostrar su valor por pantalla siempre que este
        //sea distinto de false.

            boolean[] booleans = {
                            false, true, false, true, true
            };

            for (boolean boo0lean : booleans) {
                if (boo0lean != false) {

                    System.out.println(boo0lean);
                }
            }

        //Usa un bucle apropiado para ir sumando a un número el número 3 mientras que la suma total sea menor que 20.
        //Sacar por consola la suma total en ese momento en cada iteración.

        for (int i = 0; i < 20; i += 3) {
            System.out.println("La suma de i es " + i);
        }


        //Usa un bucle apropiado para recorrer un array de cadenas de texto e ir mostrando su contenido por pantalla.
            String[] cadenasTexto = {
                    "la", "maquina", "de", "hacer", "pájaros"
            };

            for (String unaCadena : cadenasTexto) {
                System.out.println(unaCadena);
            }

        // Usar un bucle apropiado para ir recorriendo al mismo tiempo un array de booleanos y otro de números,
        // del mismo tamaño. Siempre y cuando el valor del array de booleanos no sea true y el de números un 2,
        // en ese caso terminar el bucle; en caso contrario, si el valor del array de booleanos es true,
        // sacar el número por pantalla; en caso contrario, sacar por pantalla "NO".

        boolean [] arrayDeBooleanos = {true, false, true, false, false, true};
        int [] arrayDeNumeros = {3, 55, 8, 2, 78, 12};
        i = 0;
        for (arrayDeBooleanos[i] && arrayDeNumeros[i])

    }
}

*/
    }
}